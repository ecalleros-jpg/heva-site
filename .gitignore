# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Icon?

# Videos / large media (no subir al repo)
*.mov
*.mp4
*.avi

# Images de referencia personal (no son assets del sitio)
IMG_*.PNG
IMG_*.JPG
IMG_*.HEIC

# Máster full-res de capturas de producto (el sitio usa .webp; regenerables desde los .mov)
screenshots/producto_*.png

# Brand PDF (confidencial, no va en repo público)
heva Brand Guidelines*.pdf

# Outputs temporales
/outputs/
make_og.py
